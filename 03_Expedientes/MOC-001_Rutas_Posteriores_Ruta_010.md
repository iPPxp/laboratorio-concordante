# MOC-001 - Rutas posteriores a ruta 010

ID: `MOC-NEXT-ROUTES-010`.

Fecha: 2026-07-06.

Estatus: matriz provisional de rutas posteriores.

Decision asociada: `D-2026-07-06-008`.

## Punto de partida

`MOC-ROUTE-009` queda ejecutada por `D-2026-07-06-005` como paquete documental de registro/auditoria.

Quedan preparados:

- `MOC-PILOT-NODATA-REG-001`;
- `MOC-PILOT-AUDIT-MATRIX-001`;
- `MOC-PILOT-REG-AUDIT-PACK-001`.

La ejecucion real del piloto permanece bloqueada.

## Decision de ruta

La ruta posterior mas defensible no es ejecutar piloto, reclutar evaluadores ni recopilar respuestas reales.

La ruta posterior elegida es:

```text
MOC-ROUTE-011 = mantenimiento teorico-operativo sin ejecucion
```

Esta ruta conserva `MOC-001` abierto como expediente integrador provisional, pero lo retira del frente operativo inmediato.

## Acciones permitidas en MOC-ROUTE-011

- Mantener el paquete pre-ejecucion y de registro/auditoria como referencia documental.
- Usar `MOC-EVAL-001` solo como simulador no mutante sobre casos sinteticos no clinicos.
- Refinar semantica, tabla de operaciones o protocolo solo por decision posterior.
- Preparar relacion documental con `C-001` / `C-002` sin modificar Nivel C.
- Coordinar deuda conceptual con `AO-001` y `TCS-001` sin cerrar sus problemas globales.

## Acciones bloqueadas

- Ejecutar piloto empirico real.
- Reclutar evaluadores.
- Recopilar respuestas reales.
- Recopilar datos personales o metadatos identificables.
- Evaluar personas reales.
- Usar dominios clinicos, patologicos, juridicos, financieros o institucionales regulados.
- Admitir `H-Xi`.
- Canonizar `Xi`.
- Promover MOC a Canon, Documento oficial o Nivel C.
- Modificar `Documento 04`.
- Cerrar Confluencia global o Equivalencia global.
- Exportar R4/Gamma.

## Rutas futuras validas

Las rutas futuras validas requieren decision separada:

1. `MOC-ROUTE-011`: mantenimiento teorico-operativo sin ejecucion.
2. `MOC-ROUTE-012`: relacion documental controlada con `C-001` / `C-002`, sin modificar Nivel C.
3. `MOC-ROUTE-013`: simulacion documental adicional con casos sinteticos, sin personas reales.
4. `MOC-ROUTE-014`: nueva compuerta de piloto si algun dia se propone ejecucion real.

## Rutas no validas

No son rutas validas desde el estado actual:

- saltar directo a ejecucion empirica;
- presentar el MOC como validado empiricamente;
- usar el paquete de registro como autorizacion;
- usar `Xi_eval` como operador general vigente;
- usar `MOC/AO` para cerrar globalmente Confluencia o Equivalencia;
- usar MOC para promover `REPORT_LAYER`, R4 o Gamma.

## Relacion con cierres de expedientes

La limpieza de expedientes tecnicos puede proceder en paralelo si cada cierre conserva sus deudas reales:

- `R001-001` puede cerrarse tecnicamente como herramienta local integrada.
- `AUT-002` puede cerrarse tecnicamente como tratamiento minimo de referencias historicas transferidas.
- `AUD-001` puede cerrarse operativamente en version v0, conservando rutas futuras bajo decision separada.
- `HXI-001` puede cerrarse en mantenimiento local, conservando `Xi_eval` solo como herramienta local no canonica.

## Resultado operativo

`MOC-ROUTE-010` decide que el MOC entra en mantenimiento teorico-operativo sin ejecucion.

`MOC-001` permanece abierto como expediente integrador provisional, pero no autoriza reclutamiento, piloto, personas reales, datos personales, uso clinico ni promocion canonica.
