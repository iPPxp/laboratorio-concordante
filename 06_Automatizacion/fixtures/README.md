# Fixtures de automatizacion

Estatus: fixtures ejecutables no mutantes.

Esta carpeta conserva entradas externas para validar herramientas de `06_Automatizacion`.

## Auditor v0

- `auditor_v0_cases.json`: matriz externa completa `AUD-T00` a `AUD-T09`.
- `auditor_v0_case_schema.json`: esquema operativo inicial `AUDITOR-V0-CASE-SCHEMA-001`.
- `auditor_v0_documental_cases.json`: fixture parcial documental con casos no automata; no busca conformidad `C-002` completa porque omite la matriz obligatoria.

Los fixtures no autorizan transformaciones, no sustituyen decisiones registradas y no cierran expedientes.
